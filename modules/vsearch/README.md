# My Python Project
A clean template for starting Python utility apps.

## Installation
Go to the folder containing vsearch-0.1.0.tar.gz distribution and then execute the following command:
Run 'pip install vsearch-0.1.0.tar.gz -r requirements.txt'